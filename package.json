{
  "name": "conad.it",
  "version": "1.0.0",
  "description": "Scraper for conad.it",
  "main": "main.js",
  "scripts": {
    "inst": "rm -rf node_modules && rm package-lock.json && npm install",
    "inst-win": "rmdir /S /Q node_modules && del package-lock.json && npm install"
  },
  "license": "MIT",
  "dependencies": {
    "@mikosoft/echo": "^1.0.1",
    "@mikosoft/functionflow": "^1.0.5",
    "pptr-plus": "^1.0.2",
    "puppeteer-core": "^12.0.1"
  }
}